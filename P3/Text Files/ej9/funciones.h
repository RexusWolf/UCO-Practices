#ifndef FUNCIONES_H
#define FUNCIONES_H

      #include <stdio.h>
      #include <stdlib.h>
      #include <string.h>
      FILE * openread(char * namefile);
      int stockLibro(char* namefile, char *titulo);
      char* cambiabarra (char* nombre);
#endif
