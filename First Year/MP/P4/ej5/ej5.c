#include "funciones.h"
#include "pilas.h"
int main(int argc, char const *argv[]) {
int num;
printf("Introduce numero de contenedores: ");
scanf("%d", n );
char nombre[20];
printf("Introduce n de contenedor:");
scanf("%d\n", num);
struct pila *contenedores;
pilaVacia(contenedores)
push(*contenedores, n );
//struct pila *contenedor= nuevoElemento(n);



//verCima(contenedor, nombre );
//printf("%s\n",nombre);
liberarMemoria(contenedores);
  return 0;
}
